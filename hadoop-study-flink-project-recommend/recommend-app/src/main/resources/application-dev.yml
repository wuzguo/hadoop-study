spring:
  datasource:
    username: root
    password: 136501412
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://hadoop003/recommend?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      dialect: org.hibernate.dialect.MySQL8Dialect
      storage_engine: innodb
      format_sql: true

## hbase config
hbase:
  rootdir: hdfs://hadoop001:8020/hbase
  zookeeper:
    quorum: hadoop001,hadoop002,hadoop003
  client:
    scanner:
      timeout:
        period: 100000
  rpc:
    timeout: 10000

## redis config
redis:
  host: 10.20.0.92
  port: 6379
  db: 0

## kafka config
kafka:
  bootstrap:
    servers: 10.20.0.92:9092
  zookeeper:
    connect: 10.20.0.92:2181

## data path
rating:
  data:
    path= ratings.csv